export type User = {
    name:string;
    email:string;
  }

  export type Producto = {
    id: string;
    nombre: string;
    precio: number;
    imagen: string;
    categoria: string;
    descripcion: string;
}